# Built by nix-direnv when you cd in. Holds the GC root for this
# project's dev shell.
.direnv/

# nix build output symlink.
result
result-*

# The stamp the shellHook's bootstrap example writes, if you uncomment it.
.dev-shell-deps
